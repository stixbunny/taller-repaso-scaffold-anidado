<%= form_with(model: playlist, local: true) do |form| %>
  <% if playlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(playlist.errors.count, "error") %> prohibited this playlist from being saved:</h2>

      <ul>
      <% playlist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :playlist_name %>
  </div>

  <!-- <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :playlist_user_id %>
  </div> -->

  <div class="field">
    <%= form.label :user_id %>
    <!-- <%= form.text_field :category_id, id: :product_category_id %> -->
    <%= form.select :user_id, User.all.collect{|x| [x.name, x.id]}, id: :playlist_user_id, prompt: "Seleccione usuario" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
